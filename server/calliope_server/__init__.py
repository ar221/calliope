"""Calliope dictation server package.

Extracted stage by stage from the single-file `calliope-server` script,
which remains the executable entry point and composition root.
"""

__version__ = "0.1.0"
